# OpenDig 环境变量配置示例
# 复制此文件为 .env.local 并修改相应配置

# ===========================================
# 必需配置
# ===========================================

# dig工具的完整路径
# Windows 示例:
BIND_PATH=D:\bind9\bin\dig.exe

# Linux 示例:
# BIND_PATH=/usr/bin/dig

# macOS 示例:
# BIND_PATH=/usr/local/bin/dig

# 如果 dig 在系统 PATH 中，可以简单设置为:
# BIND_PATH=dig

# ===========================================
# 可选配置
# ===========================================

# 默认DNS服务器
# 如果不设置，将使用 223.5.5.5
DEFAULT_DNS=223.5.5.5

# 调试模式
# 设置为 true 启用详细日志输出
DEBUG=false

# ===========================================
# Windows 路径配置说明
# ===========================================

# bind 9.16.39 是 bind 最后一个 Windows 版本，更新版本似乎不再发行 Windows 版本。具体请查看 Bind 官方网站。

# 1. 使用完整绝对路径，包含 .exe 扩展名
# BIND_PATH=D:\bind9\bin\dig.exe

# 2. 如果路径包含空格，使用引号
# BIND_PATH="C:\Program Files\ISC BIND 9\bin\dig.exe"

# 3. 常见安装位置：
# - BIND 9 官方安装: C:\Program Files\ISC BIND 9\bin\dig.exe
# - 手动安装: D:\bind9\bin\dig.exe
# - Chocolatey 安装: C:\ProgramData\chocolatey\bin\dig.exe

# ===========================================
# Linux/macOS 路径配置说明
# ===========================================

# 1. 系统默认位置
# BIND_PATH=/usr/bin/dig

# 2. Homebrew 安装 (macOS)
# BIND_PATH=/usr/local/bin/dig

# 3. 如果 dig 在 PATH 中
# BIND_PATH=dig

# ===========================================
# 验证配置
# ===========================================

# 配置完成后，可以通过以下方式验证：
# 1. 启动应用后访问 http://localhost:3000/api/status
# 2. 查看控制台输出的启动信息
# 3. 启用 DEBUG=true 查看详细日志
